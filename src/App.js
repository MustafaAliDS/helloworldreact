import Header from "./components/Header";

// On line 8, it can be a self closing tag since there is no content in it

function App() {
  return (
    <div className="container">
      <Header title="Task Tracker" />
    </div>
  );
}

export default App;
